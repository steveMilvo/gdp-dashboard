{
  "name": "mildura-colony-on-the-murray",
  "version": "0.1.0",
  "private": true,
  "description": "Mildura: Colony on the Murray \u2014 a real-history strategy game (M1 map slice).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --test tests/sync.test.ts",
    "start": "serve -s dist -l ${PORT:-4173}"
  },
  "devDependencies": {
    "@types/three": "^0.161.2",
    "playwright": "^1.60.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "serve": "^14.2.6",
    "three": "^0.161.0"
  }
}
