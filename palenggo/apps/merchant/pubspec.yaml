name: palenggo_merchant
description: PalengGo — Merchant app (stall owner, sari-sari, carinderia).
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  cupertino_icons: ^1.0.8
  firebase_auth: ^5.3.0
  firebase_core: ^3.6.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  palenggo_shared:
    path: ../../packages/palenggo_shared
  uuid: ^4.5.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
